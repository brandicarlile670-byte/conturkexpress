"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function AdminLogin(){const router=useRouter();const [username,setUsername]=useState("");const [password,setPassword]=useState("");const [message,setMessage]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setMessage("");const r=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username,password})});if(!r.ok){const body=await r.json().catch(()=>null);setMessage(body?.error||"Unable to sign in.");return;}router.push("/admin/tracking");router.refresh();}return <div className="ct-admin-login"><div className="login-container"><div className="brand"><svg className="logo-svg" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg"><circle cx="45" cy="45" r="25" stroke="var(--brand-color)" strokeWidth="8" fill="none"/><line x1="62" y1="62" x2="85" y2="85" stroke="var(--brand-color)" strokeWidth="8" strokeLinecap="round"/><circle cx="45" cy="45" r="10" fill="var(--brand-color)"/><path d="M20,60 L10,70" stroke="var(--brand-color)" strokeWidth="4" strokeLinecap="round"/></svg><h1>PARCEL TRACKING <span className="admin-tag">Admin</span></h1></div>{message?<div className="msg-box visible">{message}</div>:null}<form onSubmit={submit}><div className="form-group"><label htmlFor="adminUsername">Admin ID / Email</label><input type="text" id="adminUsername" placeholder="Enter your admin credentials" value={username} onChange={e=>setUsername(e.target.value)} required/></div><div className="form-group"><label htmlFor="adminPassword">Password</label><input type="password" id="adminPassword" placeholder="••••••••" value={password} onChange={e=>setPassword(e.target.value)} required/></div><button type="submit" className="btn btn-primary">Secure Login</button></form></div></div>}
